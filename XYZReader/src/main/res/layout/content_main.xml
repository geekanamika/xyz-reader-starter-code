<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.SwipeRefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:id="@+id/swipe_refresh_layout"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    app:layout_behavior="@string/appbar_scrolling_view_behavior">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:clipToPadding="false"
        android:paddingEnd="@dimen/padding_recycler_view"
        android:paddingLeft="@dimen/padding_recycler_view"
        android:paddingRight="@dimen/padding_recycler_view"
        android:paddingStart="@dimen/padding_recycler_view"
        android:paddingTop="@dimen/padding_recycler_view"
        android:paddingBottom="@dimen/padding_recycler_view"
        tools:listitem="@layout/list_item_article"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</android.support.v4.widget.SwipeRefreshLayout>